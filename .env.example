# 数据库密码(compose 内部用)
POSTGRES_PASSWORD=change-me-pg
# 对外端口
TOWER_PORT=8080
# 号库凭据加密主密钥:base64 的 32 字节。生成: openssl rand -base64 32
TOWER_MASTER_KEY=
# 会话签名密钥(>=32 字符随机)。生成: openssl rand -hex 32
TOWER_SESSION_SECRET=
# 首次启动自动创建的管理员(库为空时生效)
TOWER_ADMIN_USER=admin
TOWER_ADMIN_PASSWORD=change-me-admin
